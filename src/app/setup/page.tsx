import AppShell from "@/components/layout/AppShell";
import SetupClient from "./SetupClient";

// This page is for users who are authenticated but not yet in any mess
export default function SetupPage() {
  return <SetupClient />;
}
